window.Buffer = window.Buffer || require("buffer").Buffer;
// global.Buffer = global.Buffer || require('buffer').Buffer
console.log("Buffer:", Buffer)

function ImportBuffer() {
}

export default ImportBuffer;
